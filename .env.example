# ===========================================
# Project Zap Environment Variables
# ===========================================
# Copy this file to .env.local and fill in your values

# -------------------------------------------
# WorkOS Authentication
# -------------------------------------------
# Get these from: https://dashboard.workos.com → API Keys

# Client ID (public, used in frontend)
VITE_WORKOS_CLIENT_ID=client_01XXXXXXXXXXXXXXXXXXXXXX

# API Key (secret, server-side only)
WORKOS_API_KEY=your-workos-api-key-here

# Redirect URI for OAuth callback
# Must match the redirect URI configured in WorkOS Dashboard
WORKOS_REDIRECT_URI=http://localhost:3000/api/auth/callback

# Cookie password for session encryption (min 32 characters)
# Generate with: openssl rand -base64 24
WORKOS_COOKIE_PASSWORD=your-secure-cookie-password-at-least-32-chars

# NOTE: WORKOS_CLIENT_ID must be set in Convex Dashboard, not here!
# Go to: https://dashboard.convex.dev → Your Project → Settings → Environment Variables
# Add: WORKOS_CLIENT_ID = client_01XXXXXXXXXXXXXXXXXXXXXX (same value as above)

# -------------------------------------------
# Convex Database
# -------------------------------------------
# These are auto-generated by `bunx convex dev`
# Get manually from: https://dashboard.convex.dev

# Deployment identifier (format: dev:project-name-123)
CONVEX_DEPLOYMENT=dev:your-project-name

# Public Convex URL (used in frontend)
VITE_CONVEX_URL=https://your-project-name.convex.cloud
